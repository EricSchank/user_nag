// Place all the styles related to the home controller here.
// They will automatically be included in application.css.
// You can use Sass (SCSS) here: http://sass-lang.com/

@import "materialize/components/color";
$primary-color: color("blue", "lighten-2") !default;
@import 'materialize';

.space-it {
  margin: 40px 0;
}

.fullpage {
  .instructions i {
    margin-left: 5px;
  }
  .nag-text {
    label {
      top: 3.25em;
      left: auto;
    }
    textarea {
      border-left: 3px solid color('teal', 'lighten-1');
      padding: 10px 7px;
    }
  }
  & > ul.collapsible.popout {
    padding: 0 7px;
  }
}

.ticket {
  .nag-history {
    h5 > i {
      margin: 0 5px;
    }
    .ticket-id {
      display: none;
    }
  }
}

#toast-container {
  @media #{$medium-only} {
    min-width: 8%;
    top: 10%;
    right: 7%;
    left: 7%;
    bottom: auto;
  }
}

.toast {
  @media #{$medium-only} {
    float: right;
  }
}
